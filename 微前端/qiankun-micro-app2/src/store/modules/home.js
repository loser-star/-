import { defineStore } from "pinia";

const useHospitalDetailStore = defineStore({
  id: "Home",
  state: () => ({
    hospitalInfo: {},
  }),
  getters: {},
  actions: {},
});

export default useHospitalDetailStore;
