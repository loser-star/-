import styled from "styled-components";

export const HeaderWrapper = styled.div`
  width: 100%;
  .yourPrefix-layout.yourPrefix-layout-has-sider {
    height: Calc(100vh - 60px);
  }
  .yourPrefix-layout-header {
    padding: 0;
    height: 60px;
  }
`;
